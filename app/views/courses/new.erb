<h2>Hello, <%= @current_student.name%></h2>
<h3>Please select a course you would like to enroll in:</h3>


<form>
<% Course.all.each do |c| %>
    <input type="checkbox" name="courses" id="<%= c.id %>" value="<%= c.id %>"><%= c.name %></input><br>
  <% end %>
  <br>
  <input type="submit" value="submit">
</form>


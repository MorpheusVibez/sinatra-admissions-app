<h2>Welcome, <%= @current_student.name%></h2>
<h3>Please select a course you would like to enroll in:</h3>



<% Course.all.each do |c| %>
    <input type="checkbox" name="student[course_ids][]" id="<%= c.id %>" value="<%= c.id %>"><%= c.name %></input><br>
  <% end %>
  <br>
  <input type="submit" value="submit">

